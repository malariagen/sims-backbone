    put:
      tags:
        - OriginalSample
      summary: merges two OriginalSamples
      description: 'merges original samples with compatible properties updating references and merging sampling events'
      operationId: mergeOriginalSamples
      produces:
        - application/json
      parameters:
        - name: into
          in: path
          description: name of property to search
          required: true
          type: string
          #format: uuid
        - name: merged
          in: path
          description: matching value of property to search
          required: true
          type: string
          #format: uuid
      responses:
        '200':
          description: successful operation
          schema:
            $ref: ./swagger/definitions/original_sample.yaml#/definitions/OriginalSample
