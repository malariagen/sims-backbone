    get:
      tags:
        - location
      summary: fetches locations
      description: ''
      operationId: downloadLocations
      produces:
        - application/json
      parameters:
        - name: studyName
          in: query
          description: restrict to a particular study
          required: false
          type: string
        - name: start
          in: query
          description: for pagination start the result set at a record x
          required: false
          type: integer
        - name: count
          in: query
          description: for pagination the number of entries to return
          required: false
          type: integer
        - name: orderby
          in: query
          description: how to order the result set
          required: false
          type: string
          default: location
          enum:
            - location
            - study_name
      responses:
        '200':
          description: successful operation
          schema:
            $ref: './swagger/definitions/location.yaml#/definitions/Locations'
