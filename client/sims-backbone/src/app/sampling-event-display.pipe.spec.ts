import { SamplingEventDisplayPipe } from './sampling-event-display.pipe';

describe('SamplingEventDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new SamplingEventDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
