import { DerivativeSampleDisplayPipe } from './derivative-sample-display.pipe';

describe('DerivativeSampleDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new DerivativeSampleDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
