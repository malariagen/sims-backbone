import { OriginalSampleDisplayPipe } from './original-sample-display.pipe';

describe('OriginalSampleDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new OriginalSampleDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
