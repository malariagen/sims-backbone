    post:
      tags:
        - release
      summary: Adds an item  to a release
      description: ""
      operationId: createReleaseItem
      parameters:
        - name: release_id
          in: path
          description: ID of release to modify
          required: true
          schema:
            type: string
      requestBody:
        $ref: "./sims-backbone.yaml#/components/requestBodies/ReleaseItem"
      responses:
        "201":
          description: successful operation
        "404":
          description: Release or sample not found
    delete:
      tags:
        - release
      summary: deletes a release_item from an release
      description: ""
      operationId: deleteReleaseItem
      parameters:
        - name: release_id
          in: path
          description: ID of release to modify
          required: true
          schema:
            type: string
        - name: release_item_id
          in: path
          description: ID of release_item to remove from the set
          required: true
          schema:
            type: string
      responses:
        "200":
          description: successful operation
        "404":
          description: Release or release item not found
