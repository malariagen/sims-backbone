    post:
      tags:
        - release
      summary: Adds a note to an release
      description: ""
      operationId: createReleaseNote
      parameters:
        - name: release_id
          in: path
          description: ID of release to modify
          required: true
          schema:
            type: string
        - name: note_id
          in: path
          description: ID of note to modify in the set
          required: true
          schema:
            type: string
      requestBody:
        $ref: "./sims-backbone.yaml#/components/requestBodies/ReleaseNote"
      responses:
        "201":
          description: successful operation
        "401":
          description: Release or samplingEvent not found
    put:
      tags:
        - release
      summary: Adds a note to an release
      description: ""
      operationId: updateReleaseNote
      parameters:
        - name: release_id
          in: path
          description: ID of release to modify
          required: true
          schema:
            type: string
        - name: note_id
          in: path
          description: ID of note to modify in the set
          required: true
          schema:
            type: string
      requestBody:
        $ref: "./sims-backbone.yaml#/components/requestBodies/ReleaseNote"
      responses:
        "200":
          description: successful operation
        "404":
          description: Release not found
    delete:
      tags:
        - release
      summary: deletes an release note
      description: ""
      operationId: deleteReleaseNote
      parameters:
        - name: release_id
          in: path
          description: ID of release to modify
          required: true
          schema:
            type: string
        - name: note_id
          in: path
          description: ID of note to remove from the set
          required: true
          schema:
            type: string
      responses:
        "200":
          description: successful operation
        "404":
          description: Release or note not found
