    post:
      tags:
        - assayData
      description: Create a AssayData
      operationId: createAssayDatum
      requestBody:
        $ref: "./sims-backbone.yaml#/components/requestBodies/AssayDatum"
        description: The assay data to create
      responses:
        "200":
          description: successful operation
          content:
            application/json:
              schema:
                $ref: "./sims-backbone/definitions/assay_data.yaml#/components/schemas/AssayDatum"
