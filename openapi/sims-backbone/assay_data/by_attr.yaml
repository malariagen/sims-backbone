    get:
      tags:
        - assayData
      summary: fetches one or more AssayData by property value
      description: ""
      operationId: downloadAssayDataByAttr
      parameters:
        - name: propName
          in: path
          description: name of property to search
          required: true
          schema:
            type: string
        - name: propValue
          in: path
          description: matching value of property to search
          required: true
          schema:
            type: string
        - name: studyName
          in: query
          description: if you want to restrict the search to a study e.g. for partner_id
          required: false
          schema:
            type: string
      responses:
        "200":
          description: successful operation
          content:
            application/json:
              schema:
                $ref: "./sims-backbone/definitions/assay_data.yaml#/components/schemas/AssayData"
