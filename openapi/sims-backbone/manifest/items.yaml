    get:
      tags:
        - manifest
      summary: fetches manifests
      description: ""
      operationId: downloadManifests
      responses:
        "200":
          description: successful operation
          content:
            application/json:
              schema:
                $ref: './sims-backbone/definitions/manifest.yaml#/components/schemas/Manifests'

