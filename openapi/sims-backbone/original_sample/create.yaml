    post:
      tags:
        - OriginalSample
      description: Create a OriginalSample
      operationId: createOriginalSample
      requestBody:
        $ref: "./sims-backbone.yaml#/components/requestBodies/OriginalSample"
        description: The original sample to create
        required: true
      responses:
        "200":
          description: successful operation
          content:
            application/json:
              schema:
                $ref: "./sims-backbone/definitions/original_sample.yaml#/components/schemas/OriginalSample"
